{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4 mb-4">Create New User</h1>

        <div class="row">
            <div class="col-md-6">
                {{ include('user/_form.html.twig') }}
            </div>
        </div>

        <a class="btn btn-secondary mt-3" href="{{ path('app_user_index') }}">Back to List</a>
    </div>
{% endblock %}
